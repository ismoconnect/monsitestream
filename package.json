{
  "name": "monsitestream-monorepo",
  "private": true,
  "scripts": {
    "dev:client": "cd client-app && npm run dev",
    "dev:admin": "cd admin-app && npm run dev",
    "dev": "npm run dev:client & npm run dev:admin",
    "install:all": "npm install && cd client-app && npm install && cd ../admin-app && npm install"
  }
}
